from kivy.metrics import dp


class Theme:

    # =====================================================
    # Window
    # =====================================================

    WINDOW_WIDTH = 1280
    WINDOW_HEIGHT = 720

    # =====================================================
    # Radius
    # =====================================================

    CARD_RADIUS = dp(22)
    BUTTON_RADIUS = dp(16)
    INPUT_RADIUS = dp(14)

    # =====================================================
    # Card Size
    # =====================================================

    CARD_WIDTH = dp(300)
    CARD_HEIGHT = dp(185)

    # =====================================================
    # Padding
    # =====================================================

    SCREEN_PADDING = dp(30)
    CARD_PADDING = dp(20)

    # =====================================================
    # Spacing
    # =====================================================

    CARD_SPACING = dp(12)
    GRID_SPACING = dp(20)

    # =====================================================
    # Elevation
    # =====================================================

    CARD_ELEVATION = 3
    CARD_ELEVATION_HOVER = 10

    # =====================================================
    # Animation
    # =====================================================

    FAST_ANIMATION = 0.08
    NORMAL_ANIMATION = 0.18
    SLOW_ANIMATION = 0.30

    # =====================================================
    # Hover
    # =====================================================

    HOVER_DISTANCE = 10
    HOVER_FACTOR = 0.04

    # =====================================================
    # Glass
    # =====================================================

    BORDER_WIDTH = 1.2
    GLOW_WIDTH = 2.8

    SHINE_WIDTH = dp(80)

    # =====================================================
    # Blur (Future)
    # =====================================================

    BLUR_STRENGTH = 18

    # =====================================================
    # Fonts
    # =====================================================

    TITLE_SIZE = "H3"
    CARD_TITLE_SIZE = "H5"

    # =====================================================
    # Colors
    # =====================================================

    PRIMARY_BG = (
        0.08,
        0.11,
        0.18,
        0.92
    )

    PRIMARY_BG_HOVER = (
        0.13,
        0.17,
        0.28,
        0.96
    )

    GLASS_FILL = (
        1,
        1,
        1,
        0.06
    )

    BORDER_COLOR = (
        1,
        1,
        1,
        0.12
    )

    SHINE_COLOR = (
        1,
        1,
        1,
        0.05
    )

    TEXT = (
        1,
        1,
        1,
        1
    )

    SECONDARY_TEXT = (
        0.82,
        0.85,
        0.90,
        1
    )

    ACCENT = (
        0.30,
        0.62,
        1.00,
        1
    )